#include <stdio.h>
/**
 * main - A program that print a line using the printf function
 * Return: 0 (Success)
 * /
int main (void)
{
	printf("with proper grammer but bt theoutcome is a work of art,\n")
	return (0);
}
